- title "Envio de recomendações"
- users.each do |u|
  %ul[u]  
    - x = current_user.recommendations_count_to(u)
    %li 
      - if x < 5
        %strong
          = "Envie #{5-x} recomendações para"
          = link_to u.name, user_ratings_path(u)
      - else
        %i
          = "Já foram enviadas #{x} recomendações para #{u.name}"
      